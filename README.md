# azure-databricks-lakehouse
Azure Data Factory → Databricks (PySpark) → Delta Lake medallion pipeline (Bronze/Silver/Gold) with incremental MERGE and OPTIMIZE/ZORDER, served via Synapse Serverless SQL for Power BI.
